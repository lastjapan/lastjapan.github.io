module.exports = function() {

  $(document).ready(function($) {

    var getId = function(id) {
      var id = $(this).attr('data-id');
      return id
    };

    var getPageId = $('.works__content-area').getId();

    if (getPageId === )

  });

}
